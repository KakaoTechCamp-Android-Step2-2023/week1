<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Week1"
        tools:targetApi="33">
        <activity
            android:name=".compose.ComposeContactDetailActivity"
            android:exported="false"
            android:label="@string/title_activity_compose_contact_detail_activity"
            android:theme="@style/Theme.Week1" />
        <activity
            android:name=".compose.ComposeAddContactActivity"
            android:exported="false"
            android:label="@string/title_activity_compose_add_contact"
            android:theme="@style/Theme.Week1" />
        <activity
            android:name=".compose.ComposeMainActivity"
            android:exported="false"
            android:label="@string/title_activity_compose_main"
            android:theme="@style/Theme.Week1" />
        <activity
            android:name=".xml.ContactDetailActivity"
            android:exported="false" />
        <activity
            android:name=".xml.AddContactActivity"
            android:exported="false" />
        <activity
            android:name=".xml.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>